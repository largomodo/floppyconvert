# floppyconvert/ (resource bundle)

## Files

| File                       | What                                                                                                                            | When to read                                                                       |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| `floppyconvert.properties` | Maven-filtered properties (`application.name`, `application.version`, `application.url`) consumed by Picocli `resourceBundle`   | Changing CLI metadata displayed in `--help`, debugging missing property injection  |
